$kind: http-request
name: Get All Ride Requests
method: GET
url: '{{baseUrl}}/api/ride-requests'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
queryParams:
  - key: status
    value: PENDING
    disabled: true
