$kind: http-request
name: Get Current Ride Request
method: GET
url: '{{baseUrl}}/api/ride-requests/current'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
