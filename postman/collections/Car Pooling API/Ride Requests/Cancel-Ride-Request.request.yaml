$kind: http-request
name: Cancel Ride Request
method: POST
url: '{{baseUrl}}/api/ride-requests/{{rideRequestId}}/cancel'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
